---
title: Portal de Cliente
description: Genera URLs seguras del portal para clientes
---

## Obtener URL del Portal

### External ID (Recomendado)

```typescript
const portal = await commet.portal.getUrl({
  externalId: 'user_123'
})

console.log(portal.data.portalUrl)
```

### Customer ID

```typescript
const portal = await commet.portal.getUrl({
  customerId: 'cus_abc123'
})
```

### Email

```typescript
const portal = await commet.portal.getUrl({
  email: 'customer@example.com'
})
```

## Ejemplos de Integración

### Redirección al Portal

```typescript
export async function GET(request: Request) {
  const userId = getUserFromSession(request)

  const portal = await commet.portal.getUrl({
    externalId: userId
  })

  if (portal.data) {
    return Response.redirect(portal.data.portalUrl)
  }
}
```

### Express.js

```typescript
app.post('/api/portal', async (req, res) => {
  const portal = await commet.portal.getUrl({
    externalId: req.body.customerId
  })

  res.json({ url: portal.data.portalUrl })
})
```

## Respuesta

```typescript
{
  success: true,
  data: {
    portalUrl: "https://app.commet.co/acme/portal/overview?token=..."
  }
}
```

## Recursos Relacionados

- [Clientes](/es/docs/library/features/customers) - Gestionar datos de clientes
- [Suscripciones](/es/docs/library/features/subscriptions) - Ver lo que los clientes ven en el portal
