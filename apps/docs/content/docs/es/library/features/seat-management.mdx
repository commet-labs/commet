---
title: Seats
description: Gestiona licencias basadas en seats
---

Los cambios de seats se facturan al final del período (modelo true-up).

## Agregar

```typescript
await commet.seats.add({
  externalId: 'user_123',
  seatType: 'editor',
  count: 5,
})
```

## Remover

```typescript
await commet.seats.remove({
  externalId: 'user_123',
  seatType: 'editor',
  count: 2,
})
```

## Establecer

Establece la cantidad exacta - usa cuando sincronizas desde tu sistema.

```typescript
await commet.seats.set({
  externalId: 'user_123',
  seatType: 'editor',
  count: 10,
})
```

## Obtener Balance

```typescript
const balance = await commet.seats.getBalance({
  externalId: 'user_123',
  seatType: 'editor',
})
```

## Relacionado

- [Subscriptions](/docs/library/features/subscriptions)
- [Features](/docs/library/features/features)
