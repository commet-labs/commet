# Add these lines to your .gitignore when using Commet CLI

# Commet CLI configuration (contains sensitive org info)
.commet

# Commet authentication credentials (NEVER commit this!)
.commet/

# Auto-generated type definitions (can be regenerated with `commet pull`)
.commet.d.ts

