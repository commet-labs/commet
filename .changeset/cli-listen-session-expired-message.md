---
"commet": patch
---

`commet listen` now shows a clear "Your session expired" message and suggests running `commet login` when the session is no longer valid, instead of a generic "Failed to start listen session" error.
