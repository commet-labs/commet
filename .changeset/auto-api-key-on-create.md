---
"commet": minor
---

Automatically create an API key and configure `.env` during `commet create`, removing the manual step of creating a key in the dashboard. Also restricts `commet create` to sandbox environment only.
