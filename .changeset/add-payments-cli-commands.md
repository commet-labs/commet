---
"commet": minor
---

Add `payments` commands (`create`, `charge`, `get`, `list`, `cancel`) and subscription recovery commands (`reactivate`, `create-recovery-link`, `update-payment-method`) to the CLI.
