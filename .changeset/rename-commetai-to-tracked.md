---
"@commet/ai-sdk": minor
---

Rename `commetAI` to `tracked` for better readability. The function wraps an AI SDK model to track token usage, and the name now reflects what it returns: a tracked model.
