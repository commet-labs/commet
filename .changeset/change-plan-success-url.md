---
"@commet/node": minor
---

Add `successUrl` to `subscriptions.changePlan()`. When a free→paid plan change routes through checkout, the caller can now pass where to redirect after the payment completes.
