---
"@commet/node": minor
---

Add `customIntroOffer` to `subscriptions.create` to pre-apply a custom introductory offer (discountType, discountValue, durationCycles) when assigning a plan, overriding the plan's configured intro offer.
