---
"@commet/node": minor
---

Add the `payments` resource for one-time payments and payment links (`create`, `charge`, `get`, `list`, `cancel`), plus subscription recovery methods: `subscriptions.reactivate`, `subscriptions.createRecoveryLink`, and `subscriptions.updatePaymentMethod`.
